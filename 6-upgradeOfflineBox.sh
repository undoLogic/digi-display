#!/usr/bin/env bash

# tell server we are starting an upgrade
## allowing to allert someone is server doesn't check back in after upgrade is complete

#apt update ???

# a restore proceedure ? if we can't contact server after upgrade ?